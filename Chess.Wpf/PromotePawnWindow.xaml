<Window x:Class="Chess.Wpf.PromotePawnWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Pawn promote" Height="199" Width="283">
    <Window.Resources>

        <Style TargetType="Button">
            <Setter Property="FontSize" Value="40" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
        </Style>

    </Window.Resources>
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button x:Name="btnQueen" Grid.Row="0" Grid.Column="0" Content="Q" Click="Queen_Click" />
        <Button x:Name="btnRook" Grid.Row="0" Grid.Column="1" Content="R" Click="Rook_Click" />
        <Button x:Name="btnBishop" Grid.Row="1" Grid.Column="0" Content="B" Click="Bishop_Click" />
        <Button x:Name="btnKnight" Grid.Row="1" Grid.Column="1" Content="K" Click="Knight_Click" />

    </Grid>
</Window>
